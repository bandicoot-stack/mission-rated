(()=>{const API='https://vquwdypidgjmxnhhdbol.supabase.co/functions/v1/public-explore';const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const good=u=>/^https:\/\//i.test(String(u||''));const dt=x=>new Date(x);const dateOf=x=>x.start_at||x.event_date||x.starts_at||x.start_date||x.effective_start||x.date;const endOf=x=>x.end_at||x.ends_at||x.end_date||x.effective_end||dateOf(x);function mount(){const tabs=document.querySelector('.tabrow'),main=document.querySelector('main.main');if(!tabs||!main||document.getElementById('weekly'))return;const b=document.createElement('button');b.className='tab';b.dataset.view='weekly';b.setAttribute('aria-selected','false');b.textContent='This Week';tabs.appendChild(b);const s=document.createElement('section');s.className='section';s.id='weekly';s.hidden=true;s.innerHTML='<div class="head"><div><h2>Military Life This Week</h2><small>EVENTS • FAMILY • SUPPORT • SAVINGS</small></div><span class="countchip" id="weeklyCount"></span></div><div class="context">A weekly view of source-backed military and community opportunities. Eligibility, cost and dates are shown when available; always confirm details at the source.</div><div class="grid" id="weeklyGrid"><div class="empty">Loading this week…</div></div>';main.appendChild(s);b.addEventListener('click',()=>{document.querySelectorAll('.tab').forEach(x=>{x.classList.toggle('active',x===b);x.setAttribute('aria-selected',x===b?'true':'false')});document.querySelectorAll('.section').forEach(x=>x.hidden=x!==s);load()});}async function load(){const g=document.getElementById('weeklyGrid');if(!g)return;try{const r=await fetch(API);if(!r.ok)throw Error();const d=await r.json();const pools=['events','military_life_resources','resources','deals','support_resources'].flatMap(k=>Array.isArray(d[k])?d[k]:[]);const now=new Date(),to=new Date(now);to.setDate(to.getDate()+7);const rows=pools.filter(x=>{const a=dateOf(x),z=endOf(x);if(!a)return false;const A=dt(a),Z=dt(z);return !isNaN(A)&&A<=to&&Z>=new Date(now.getFullYear(),now.getMonth(),now.getDate())}).sort((a,b)=>dt(dateOf(a))-dt(dateOf(b))).slice(0,16);document.getElementById('weeklyCount').textContent=rows.length?`${rows.length} this week`:'Building coverage';g.innerHTML=rows.length?rows.map(x=>{const when=dt(dateOf(x));const title=x.title||x.name||x.businesses?.name||'Military life opportunity';const place=x.location_name||x.location||x.city||x.installation_name||'';const cost=x.cost_text||x.price_text||x.offer_value_text||x.value||'';const elig=x.eligibility||x.eligibility_text||x.audience||'';const u=x.source_url||x.website_url||x.url;return `<article class="card ${cost?'hot':''}"><div class="badges"><span class="badge official">${when.toLocaleDateString('en-US',{weekday:'short',month:'short',day:'numeric'})}</span>${cost?`<span class="badge good">${esc(cost)}</span>`:''}</div><h3>${esc(title)}</h3>${place?`<div class="line">📍 ${esc(place)}</div>`:''}${elig?`<div class="line">Who: ${esc(elig)}</div>`:''}<p class="muted">${esc(x.description||x.summary||'Source-backed military life opportunity.')}</p><div class="links">${good(u)?`<a class="btn" href="${esc(u)}" target="_blank" rel="noopener noreferrer">Details ↗</a>`:''}</div></article>`}).join(''):'<div class="empty">No dated items are available for the next 7 days yet. Mission Rated is building coverage.</div>'}catch{g.innerHTML='<div class="empty">Weekly intelligence is temporarily unavailable.</div>'}}mount();})();