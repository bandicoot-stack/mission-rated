{
  "name": "mission-rated",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "node scripts/build-all.mjs",
    "test": "node --test tests/*.test.mjs",
    "qa": "npm run build && node scripts/qa.mjs && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
