(async()=>{'use strict';const {SUPABASE_FUNCTIONS_ROOT}=await import('/lib/config.js');const API=SUPABASE_FUNCTIONS_ROOT+'public-auto-dealers';const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const safe=u=>/^https:\/\//i.test(String(u||''))?String(u):'';const day=v=>{if(!v)return'';const d=new Date(v);return Number.isNaN(d.getTime())?'':d.toLocaleDateString(undefined,{year:'numeric',month:'short',day:'numeric'})};const badge=(t,c='source')=>`<span class="badge ${c}">${esc(t)}</span>`;const params=new URLSearchParams(location.search),type=params.get('type')||'dealer',id=params.get('id')||'';function mr(x){return badge(`MR ${x?.mr_status==='rated'&&x?.mr_score!=null?x.mr_score:'Building'}`,'mr')}function links(items){return `<div class="links">${items.filter(x=>x[1]).map(([label,url])=>`<a href="${esc(url)}" target="_blank" rel="noopener noreferrer">${esc(label)} ↗</a>`).join('')}</div>`}async function run(){const title=document.getElementById('title'),root=document.getElementById('content');try{const r=await fetch(API);if(!r.ok)throw Error(r.status);const j=await r.json();if(type==='salesperson'){const p=(j.salespeople||[]).find(x=>x.id===id);if(!p)throw Error('not_found');title.textContent=p.name;root.classList.add('person');root.innerHTML=`<div class="badges">${mr(p)}${p.official_verified?badge('Official Source Verified','official'):''}${p.user_verified?badge('User Verified','user'):''}${p.source_url?badge(p.official_verified?'First-party evidence':'External Evidence','source'):''}</div><h2>${esc(p.name)}</h2><div class="muted">${esc(p.dealer?.name||'Dealer pending')} • ${esc(p.dealer?.city||'Hampton Roads')}</div><div class="evidence"><b>Evidence summary</b><br>${esc(p.evidence_note||'Named salesperson evidence is still building.')}</div><div class="muted" style="margin-top:10px">${p.mention_count?`${esc(p.mention_count)} sourced mention${p.mention_count===1?'':'s'} • `:''}${day(p.observed_at)?`Observed ${esc(day(p.observed_at))}.`:''} Mission Rated does not convert third-party mentions into an MR score.</div>${links([['Evidence source',safe(p.source_url)],['Dealer website',safe(p.dealer?.website_url)]])}`;return}const d=(j.dealers||[]).find(x=>x.id===id);if(!d)throw Error('not_found');title.textContent=d.name;root.innerHTML=`<div class="badges">${mr(d)}${d.official_verified?badge('Official Source Verified','official'):''}${d.website_verified?badge('Official Website','website'):''}${d.user_verified?badge('User Verified','user'):''}${d.public_rating_url?badge('Sourced Public Rating','source'):''}</div><h2>${esc(d.name)}</h2><div class="muted">${esc(d.address||`${d.city}, ${d.state}`)}${Array.isArray(d.brands)&&d.brands.length?` • ${esc(d.brands.join(', '))}`:''}</div>${d.public_rating!=null?`<div class="rating">${esc(d.public_rating)}/5</div><div class="muted">${esc(d.public_review_count??0)} public reviews • ${esc(d.public_rating_source||'source')}${day(d.public_rating_observed_at)?` • observed ${esc(day(d.public_rating_observed_at))}`:''}</div>`:'<div class="muted" style="margin-top:10px">Public rating source pending.</div>'}${d.military_signal?`<div class="evidence"><b>${d.official_military_evidence?'Official military evidence':'Military signal — verification building'}</b><br>${esc(d.military_signal)}</div>`:''}${(d.signals||[]).length?`<div class="evidence"><b>Additional sourced signals</b><br>${d.signals.map(s=>`${esc(s.signal_type||s.signal_key||'Signal')}${s.signal_text?`: ${esc(s.signal_text)}`:''}${day(s.observed_at)?` • ${esc(day(s.observed_at))}`:''}`).join('<br>')}</div>`:''}<div class="muted" style="margin-top:10px">Public ratings, review counts and external signals are evidence only. MR remains Building until Mission Rated has sufficient native evidence.</div>${links([['Public rating source',safe(d.public_rating_url)],['Military evidence source',safe(d.evidence_url)],['Official dealer website',safe(d.website_url)]])}`;}catch(e){title.textContent='Evidence unavailable';root.innerHTML='<div class="empty">This sourced detail record could not be loaded.</div>'}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run,{once:true});else run()})();