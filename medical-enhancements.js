(()=>{
'use strict';
const grid=document.getElementById('grid'),controls=document.querySelector('.controls');
if(!grid||!controls||document.getElementById('medicalSort'))return;
const style=document.createElement('style');style.textContent='.mrEvidenceFresh{border-color:#236f61!important;color:#8affdc!important;background:#08251f!important}.mrEvidenceAging{border-color:#7b672f!important;color:#ffd36d!important;background:#1a1b17!important}.mrEvidenceStale{border-color:#844!important;color:#ffadad!important;background:#281313!important}@media(min-width:901px){.controls{grid-template-columns:2fr 1fr 1fr 1fr!important}}';document.head.appendChild(style);
const sort=document.createElement('select');sort.id='medicalSort';sort.setAttribute('aria-label','Sort medical providers');sort.innerHTML='<option value="fresh">Freshest evidence</option><option value="name">Name A–Z</option><option value="city">City A–Z</option>';controls.appendChild(sort);
const age=d=>{const t=Date.parse(String(d||''));return Number.isFinite(t)?Math.max(0,Math.floor((Date.now()-t)/86400000)):999999};
const dateFromCard=card=>{const m=card.textContent.match(/TRICARE evidence checked\s+(\d{4}-\d{2}-\d{2})/i);return m?m[1]:''};
function enhance(){for(const card of grid.querySelectorAll('.card')){const badges=card.querySelector('.badges'),d=dateFromCard(card),days=age(d);if(badges&&!card.querySelector('.mrEvidenceAge')){const b=document.createElement('span');b.className='badge mrEvidenceAge '+(days<=30?'mrEvidenceFresh':days<=90?'mrEvidenceAging':'mrEvidenceStale');b.textContent=days<=30?'Evidence fresh':days<=90?`Evidence ${days}d old`:'Recheck evidence';badges.appendChild(b)}const id=card.dataset.itemId,links=card.querySelector('.links');if(id&&links&&!card.querySelector('.mrMedicalDetail')){const a=document.createElement('a');a.className='btn mrMedicalDetail';a.href=`/business?id=${encodeURIComponent(id)}`;a.textContent='Mission Rated details →';links.prepend(a)}}
 const cards=[...grid.querySelectorAll('.card')];cards.sort((a,b)=>{if(sort.value==='name')return (a.querySelector('h2')?.textContent||'').localeCompare(b.querySelector('h2')?.textContent||'');if(sort.value==='city')return (a.querySelector('.meta')?.textContent||'').localeCompare(b.querySelector('.meta')?.textContent||'')||(a.querySelector('h2')?.textContent||'').localeCompare(b.querySelector('h2')?.textContent||'');return age(dateFromCard(a))-age(dateFromCard(b))||(a.querySelector('h2')?.textContent||'').localeCompare(b.querySelector('h2')?.textContent||'')});for(const c of cards)grid.appendChild(c)}
const observer=new MutationObserver(enhance);observer.observe(grid,{childList:true,subtree:true});sort.addEventListener('change',enhance);enhance();
})();
